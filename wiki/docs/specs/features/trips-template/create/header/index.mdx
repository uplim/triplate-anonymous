import { ImageRow } from "/components/image-row";
import { Image } from "/components/image";
import sp from "./view-sp-header.png";
import pc from "./view-pc-header.png";
import backLink from "./back-link.png";

# ヘッダー

<ImageRow sp={sp} pc={pc} />

## 外部仕様

### 構成

- [バックリンク](#バックリンク)
- [リロードボタン](#リロードボタン)

### バックリンク

<Image src={backLink} size={100} />

クリックすると、必ず[マイページ](/specs/features/settings/mypage)へ遷移します。ブラウザの戻るボタンとは挙動が異なります。フォームに入力がある場合は、[確認モーダル](./confirm-modal)を表示します。

### リロードボタン
